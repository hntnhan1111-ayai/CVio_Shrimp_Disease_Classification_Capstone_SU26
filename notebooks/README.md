# Notebooks

`RECSRA_Reproduction.ipynb` is a portable, repository-relative entry point. Set `DATASET_ROOT` before evaluation. The original machine-specific notebook is retained under `archive/original_experiment_source/robustness/` for provenance.
