# Notebooks

The included notebooks are archival experiment references for the final
YOLO26m improvement workflow and the Stage-1 TIMM baseline. The portable,
supported workflow is implemented by `configs/`, `src/`, and `scripts/`.

The notebook may contain historical local paths and RTX 4090 notes; it is not
the default Kaggle T4x2 reproduction entry point.
