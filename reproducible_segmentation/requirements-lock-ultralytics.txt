# Exact environment recorded by the YOLO11n-Seg strong 200-epoch baseline.
# Install a CUDA-compatible torch wheel from the matching PyTorch index first.
ultralytics==8.4.61
torch==2.10.0
torchvision==0.25.0
numpy==2.0.2
opencv-python==4.11.0.86
pillow==11.3.0
PyYAML==6.0.2
pandas==2.2.3

