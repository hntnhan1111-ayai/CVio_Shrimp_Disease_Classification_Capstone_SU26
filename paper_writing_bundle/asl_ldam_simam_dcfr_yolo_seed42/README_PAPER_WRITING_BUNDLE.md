# Paper Writing Bundle — ASL-LDAM / SimAM-DCFR YOLO Seed 42

This folder contains the curated paper-writing bundle for the seed-42-only artifact package.

- Branch: `paper/asl-ldam-simam-dcfr-yolo-noisy-shrimp`
- Main method: YOLO26m-cls + ASL-LDAM + SimAM-DCFR
- Scope: fixed seed-42 Stage-1 split only. Do not claim mean ± std or statistical significance across seeds.
- No model weights/checkpoints. No raw datasets.

Folders:
- `title/` — paper title and scope docs
- `tables/` — all core CSVs
- `figures/` — core and appendix paper-ready figures
- `xai/` — XAI panels and metadata
- `images/` — sample images
- `source_evidence/` — corrections, sources
- `code_reference/` — lightweight runner scripts
- `validation/` — package validation and coverage CSVs

Use existing values; do not rename column headers for paper claims.
