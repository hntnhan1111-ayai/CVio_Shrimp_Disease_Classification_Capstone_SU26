# Stage-1 YOLO Baselines

Run the curated native Ultralytics baselines with:

```bash
bash scripts/02_run_yolo_baselines.sh
```

Historical path-bound runners were migrated into `src/cvio_asl_ldam/`.
