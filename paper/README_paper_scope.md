# Paper Scope

This repository packages results for the fixed seed-42 Stage-1 split only.
It does not support claims of multi-seed mean plus/minus standard deviation or
statistical significance across random seeds.

The reported runtime is Kaggle T4x2 GPU with Python 3.12.3. Raw data, trained
weights, checkpoints, and generated corrupted-image folders are not included.
