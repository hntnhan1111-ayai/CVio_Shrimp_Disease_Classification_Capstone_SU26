import type { Row } from "../lib/csv"; import { DataTable } from "../components/DataTable"; import { WarningBox } from "../components/WarningBox";
export function LabelAudit({mapping,errors}:{mapping:Row[];errors:Row[]}) { return <section id="labels"><div className="section-title"><span>02</span><div><h2>Label Audit &amp; Canonical Remapping</h2><p>Folder-local source IDs are not globally consistent.</p></div></div><div className="grid-2"><DataTable title="Source folder × canonical class" rows={mapping}/><div className="panel"><h3>Required remapping</h3><ul><li>BG folder: source class 0 → BG.</li><li>WSSV folder: source class 0 → WSSV.</li><li>WSSV_BG folder: source class 0 → WSSV; source class 1 → BG.</li></ul><WarningBox>Training directly on the original IDs would merge different biological labels and invalidate the learning target.</WarningBox></div></div>{errors.length?<DataTable title="Label errors" rows={errors}/>:<WarningBox tone="success"><strong>Audit passed:</strong> no label errors are recorded in the packaged audit table.</WarningBox>}</section>; }
