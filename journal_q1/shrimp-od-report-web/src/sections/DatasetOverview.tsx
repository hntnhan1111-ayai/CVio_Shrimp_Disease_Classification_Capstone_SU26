import type { Row } from "../lib/csv"; import { DataTable } from "../components/DataTable"; import { WarningBox } from "../components/WarningBox";
export function DatasetOverview({raw,annotated,boxes}:{raw:Row[];annotated:Row[];boxes:Row[]}) { return <section id="dataset"><div className="section-title"><span>01</span><div><h2>Dataset Overview</h2><p>Separate image-level health state from disease-region localization.</p></div></div><div className="grid-3"><DataTable title="Raw class counts" rows={raw}/><DataTable title="Annotated images" rows={annotated}/><DataTable title="Canonical box counts" rows={boxes}/></div><WarningBox tone="info"><strong>Task formulation.</strong> Healthy is image-level only and is not a bounding-box class. The detector uses canonical classes <strong>0 = BG</strong> and <strong>1 = WSSV</strong>; WSSV_BG is represented by co-occurring BG and WSSV evidence.</WarningBox></section>; }
